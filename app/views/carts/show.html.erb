<h2>Shopping Cart</h2>
<ul>
    <% @cart.line_items.each do |item| %>
        <li><%= item.product.title %></li>
    <% end %>
</ul>
<%= link_to 'Back', {:controller => 'store', :action => 'index'} %>
